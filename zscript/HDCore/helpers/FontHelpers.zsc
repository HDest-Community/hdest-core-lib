extend class HDCore {

    static clearscope int getBatteryFontColor(int charge) {
        if (charge > 13) return Font.CR_GREEN;
        if (charge > 6)  return Font.CR_YELLOW;
        if (charge > 0)  return Font.CR_RED;

        return Font.CR_GRAY;
    }
}